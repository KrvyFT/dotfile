if status is-interactive
    # Commands to run in interactive sessions can go here
end

alias img 'kitty +kitten icat'
alias diff 'kitty +kitten diff'
alias vim 'nvim'
alias ex 'extract'
alias aria 'aria2c -s 4 -x 8 -j 16 -c'
